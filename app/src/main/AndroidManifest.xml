<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.beryl.lmtestapp"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name="BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity_">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>
        <activity android:name=".WebViewProxyActivity">
        </activity>
        <activity android:name=".WindVaneViewProxyActivity">
        </activity>
        <activity android:name=".WVFragmentWebViewActivity">
        </activity>

    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.taobao.windvane.runtimepermission.PermissionActivity" />
</manifest>